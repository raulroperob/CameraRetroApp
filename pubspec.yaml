name: retro_cam
description: App de cámara retro con múltiples "cámaras"/filtros para Android.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.6
  path_provider: ^2.1.3
  image: ^4.2.0
  permission_handler: ^11.3.1
  gal: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
